// Hero.tsx
import { HeroContainer, HeroTitle } from './HeroStyles';

const Hero = () => (
  <HeroContainer>
    <div className="container">
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </div>
  </HeroContainer>
);

export default Hero;
